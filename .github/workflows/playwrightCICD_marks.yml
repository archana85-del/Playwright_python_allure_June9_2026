name: Playwright CI/CD april 18 batch with marks

on: 
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      marker:
        description: 'Enter the marker to run'
        required: true
        default: 'login'
  schedule:
    - cron: '30 22 * 6 1'    #min hour DayOfMonth month week(0=sun)  UTC
  

    
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      # Set environment variables for the workflows
      usname: ${{vars.USNAME}}
      pw: ${{secrets.PW}}
      

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout Repository from my github
        uses: actions/checkout@v4

      # 2️⃣ Set up Python version
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install allure-pytest
          playwright install --with-deps

      # 4️⃣ Run Playwright tests (parallel, keep artifacts)
      - name: Run Playwright Tests
        run: |
          pytest -n 6 -m "${{ github.event.inputs.marker }}" -s\
            --alluredir=allure-results \
            --capture=tee-sys
        continue-on-error: true

      # 5️⃣ Verify Allure Results (DEBUG SAFETY)
      - name: Verify Allure Results
        if: always()
        run: |
          echo "Listing allure-results contents:"
          ls -la allure-results || echo "No allure-results directory found"

      # 6️⃣ Install Allure CLI i
      - name: Install Allure CLI
        if: always()
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip wget
          wget https://github.com/allure-framework/allure2/releases/download/2.27.0/allure-2.27.0.zip
          unzip allure-2.27.0.zip
          sudo mv allure-2.27.0 /opt/allure
          sudo ln -s /opt/allure/bin/allure /usr/bin/allure
          allure --version

      # 7️⃣ Generate Allure HTML report (NO DATA LOSS)
      - name: Generate Allure Report
        if: always()
        run: |
          allure generate allure-results -o allure-report --clean

      # 8️⃣ Deploy to GitHub Pages
      - name: Deploy Allure Report to GitHub Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: allure-report

     